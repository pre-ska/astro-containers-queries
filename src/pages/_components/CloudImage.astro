---
const {
  src = "",
  alt = "",
  width,
  height,
  className = "",
} = {
  width: 300,
  height: 300,
  ...Astro.props,
}
---

<img src={src} alt={alt} width={width} height={height} class={className} />
